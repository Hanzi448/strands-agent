# Runtime dependencies for the tool layer, agents, and Lambda handlers.
boto3>=1.35
# The agent framework. `backend/agents/` imports it; `backend/tools/` must
# not, so a Lambda that only mutates data can install without it.
# AgentCore packages land with the deploy step.
strands-agents>=1.54
# The IANA timezone database. `zoneinfo` reads the system copy where there
# is one (Lambda, Linux containers); Windows has none, so a clinic's
# `timezone` would look misconfigured during local development without it.
tzdata>=2024.1
